#include <stdio.h>
int main() {
    int a,b;
  printf("Enter the two numbers to check ");
  scanf("%d%d",&a,&b);
  if((a^b)<0)
  {
      printf("Opposite sign ");
  }
  else
  {
      printf("Same sign ");
  }
    return 0;
}
